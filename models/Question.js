'use strict';

const db = require("../config/db")

class Question{
   static CreateTable(){
     const sql = new promise(function(resolve){
       resolve("nothing");
     })


   }
}


module.exports = Question;
